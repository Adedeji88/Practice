#include <stdio.h>

/**
 * main - sum of numbers in multi table
 * Return: Always 0;
 */

int main(void)
{
    int i, j, k, prod;
    int sum = 0;

    if(i = 8)
    {
        for(j = 1; j <= 10; j++)
        {
            prod = i * j;
            printf("%d", prod);
            putchar('\n');

            sum = sum + prod;
            printf("sum of the numbers in multiplication table is: %d", sum);
        }
    }
    return(0);
}