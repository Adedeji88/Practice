#include <stdio.h>
int main(void)
{
    int i, j, p, sum = 0;

    if(i = 8)
    {
        for(j = 1; j <= 10; j++)
        {
            p = i * j;
            sum = sum + p;
            printf("The products of are %d\n", p);
        }
        printf("The sum of the products of 8 is %d", sum);
    }
}