#include <stdio.h>

/* main - 2 dimensional array
*
* Return: Always 0
*/
int main(void)
{
    int arr_2D[2][2] = { arr_2D[0,0], arr_2D[0,1], arr_2D[1,0], arr_2D[1,1]};
    int i, j;
    arr_2D = [i,j];

    for(i = 0; i < 2; i++)
    {
        printf("Enter the array elements: ");
        scanf("[%d,%d]", i, j);
    }
    return(0);
}
