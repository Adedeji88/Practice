#include <stdio.h>

int main(void)
{
    int n;

    printf("The size of char on my computer is: %lu bytes\n", sizeof(char));
    printf("The size of int on my computer is: %lu byyes\n", sizeof(int));
    printf("The size of float on my computer is: %lu bytes\n", sizeof(float));
    printf("The size of any variable on my computer is: %lu bytes\n", sizeof(n));

    return(0);
}